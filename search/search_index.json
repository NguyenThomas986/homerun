{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"HOMERun","text":"<p>Alpha</p> <p>HOMERun is in alpha testing. Some features are still rough or subject to change.</p> <p>HOMERun is a csRNA-seq analysis pipeline designed for running on HPC clusters.</p> <p>It automates the analysis workflow from raw FASTQ files through alignment, tag directories, TSS analysis, quality control, stability analysis, and reporting.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>Supports STAR and HISAT2</li> <li>Designed for SLURM-based HPC clusters</li> <li>Automated quality control and reporting</li> <li>Parallel processing with SLURM job arrays</li> </ul>"},{"location":"#getting-started","title":"Getting Started","text":"<p>See the Installation guide to get started.</p>"},{"location":"cli/","title":"CLI Reference","text":"<p>Alpha</p> <p>HOMERun is in alpha testing. Nothing here is finalized, and some flags and behaviors are still rough or subject to change.</p> <p>Full usage for the <code>homerun</code> command. This page mirrors <code>homerun --help</code>.</p> <p>On start, <code>homerun</code> prints:</p> <pre><code>    __  __\n   / / / /___  ____ ___  ___  _______  ______\n  / /_/ / __ \\/ __ `__ \\/ _ \\/ ___/ / / / __ \\\n / __  / /_/ / / / / / /  __/ /  / /_/ / / / /\n/_/ /_/\\____/_/ /_/ /_/\\___/_/   \\__,_/_/ /_/\n\nRNA-seq analysis pipeline for HPC clusters.\nVersion: 1.0.0\n</code></pre>"},{"location":"cli/#usage","title":"Usage","text":"<pre><code>homerun [-h] [--project PROJECT] [--log-path LOG_PATH]\n        [--steps {trim,align,tagdirs,tagdirs-combo,bedgraphs,tss,ritrie,qc,stability,report} ...]\n        [--sample-index SAMPLE_INDEX] [--group-index GROUP_INDEX]\n        [--skip-prepare] [--force] [--only-prepare]\n        [--count-samples] [--count-groups] [--check-rerun] [--stage-raw]\n        [--aligner {star,hisat2}] [--genome-index GENOME_INDEX] [--genome GENOME]\n        [--gtf GTF] [--copy-src COPY_SRC] [--threads THREADS]\n        [--trim-adapter TRIM_ADAPTER] [--trim-min TRIM_MIN] [--trim-max TRIM_MAX]\n        [--ntag-threshold NTAG_THRESHOLD] [--skip-chr SKIP_CHR]\n        [--cleanup-intermediates]\n        [--star-filter-multimap STAR_FILTER_MULTIMAP]\n        [--star-multimap-out STAR_MULTIMAP_OUT]\n        [--star-multimap-order {Random,Old_2.4}]\n        [--hisat2-strandness {F,R,FR,RF}]\n</code></pre>"},{"location":"cli/#options","title":"Options","text":"Flag Description <code>-h</code>, <code>--help</code> Show the help message and exit. <code>--project PROJECT</code> Project root (default: <code>$CSRNA_PROJECT</code> or CWD). <code>--log-path LOG_PATH</code> Pipeline log file path (overrides <code>CSRNA_LOG</code>; else a timestamped file under <code>&lt;project&gt;/logs/</code>). <code>--steps STEP [STEP ...]</code> Run only these steps (still executed in canonical order). Choices: <code>trim</code>, <code>align</code>, <code>tagdirs</code>, <code>tagdirs-combo</code>, <code>bedgraphs</code>, <code>tss</code>, <code>ritrie</code>, <code>qc</code>, <code>stability</code>, <code>report</code>. <code>--sample-index N</code> 0-based index into RawData R1 files. Restricts <code>trim</code>/<code>align</code>/<code>tagdirs</code> to one leaf run. Used by <code>SLURM_ARRAY_TASK_ID</code>. <code>--group-index N</code> 0-based index into the sample list. Restricts <code>tagdirs-combo</code>/<code>bedgraphs</code>/<code>tss</code> to one Species/Sample group. Used by <code>SLURM_ARRAY_TASK_ID</code>. <code>--skip-prepare</code> Skip folder creation / raw copy / STARIndex setup. <code>--force</code>, <code>--overwrite</code> Allow rerunning on a project that already has pipeline outputs (<code>Trimmed/</code>, <code>Aligned/</code>, <code>TagDirs/</code>, <code>bedGraphs/</code>, <code>TSS/</code>, <code>QC/</code>). Without this flag, HOMERun refuses to start (and touches nothing) if any of those exist, to avoid overwriting a previous run. No effect on <code>--count-samples</code>/<code>--count-groups</code>/<code>--stage-raw</code>. <code>--only-prepare</code> Run prepare and exit. <code>--count-samples</code> Print the number of leaf runs (R1 files in RawData) and exit. <code>--count-groups</code> Print the number of Species/Sample groups and exit. <code>--check-rerun</code> Preflight for wrapper scripts: exit 1 (with a message) if every staged sample already has a populated <code>QC/</code> and <code>--force</code> was not given \u2014 i.e. the run would touch nothing new. Exit 0 otherwise. Submits/runs nothing itself; call it before submitting SLURM jobs. <code>--stage-raw</code> Move loose <code>*_R1*</code> / <code>*_R2*</code> FASTQs into <code>RawData/</code> and exit."},{"location":"cli/#config-overrides","title":"Config overrides","text":"<p>These override <code>config.env</code> values when given.</p> Flag Description <code>--aligner {star,hisat2}</code> Aligner (overrides <code>CSRNA_ALIGNER</code>). <code>--genome-index GENOME_INDEX</code> STAR genomeDir or HISAT2 prefix (overrides <code>CSRNA_GENOME_INDEX</code>). <code>--genome GENOME</code> HOMER genome (overrides <code>CSRNA_GENOME</code>). <code>--gtf GTF</code> GTF annotation file for the RIT/RIE metric (overrides <code>CSRNA_GTF</code>). <code>--copy-src COPY_SRC</code> FASTQ copy source (overrides <code>CSRNA_COPY_SRC</code>). <code>--threads THREADS</code> Threads (overrides <code>CSRNA_THREADS</code> / <code>SLURM_CPUS_PER_TASK</code>). <code>--trim-adapter TRIM_ADAPTER</code> 3\u2032 adapter sequence. <code>--trim-min TRIM_MIN</code> Minimum read length after trimming. <code>--trim-max TRIM_MAX</code> Maximum read length after trimming. <code>--ntag-threshold NTAG_THRESHOLD</code> Minimum tags for TSS calling. <code>--skip-chr SKIP_CHR</code> Chromosome excluded from bedGraphs. <code>--cleanup-intermediates</code> Delete each sample's <code>Trimmed/</code> and <code>Aligned/</code> directories once the <code>qc</code> step has generated its tables from them (overrides <code>CSRNA_CLEANUP_INTERMEDIATES</code>). Off by default."},{"location":"cli/#alignment-overrides","title":"Alignment overrides","text":"Flag Description <code>--star-filter-multimap N</code> STAR <code>outFilterMultimapNmax</code>. <code>--star-multimap-out N</code> STAR <code>outSAMmultNmax</code>. <code>--star-multimap-order {Random,Old_2.4}</code> STAR <code>outMultimapperOrder</code>. <code>--hisat2-strandness {F,R,FR,RF}</code> HISAT2 RNA strandness."},{"location":"contributing/","title":"Contributing","text":"<p>Alpha</p> <p>HOMERun is in alpha. Interfaces and layout may change, so check with the maintainer before building anything large on top of the current API.</p> <p>Thanks for helping improve HOMERun.</p>"},{"location":"contributing/#development-setup","title":"Development setup","text":"<pre><code>git clone https://github.com/NguyenThomas986/homerun.git\ncd homerun\npython -m venv venv\nsource venv/bin/activate        # Windows: venv\\Scripts\\activate\npip install -e \".[test]\"\n</code></pre> <p>The editable install puts the <code>homerun</code> command on your <code>PATH</code> and lets the test suite import the package. (The test suite also adds the repo root to <code>sys.path</code> itself, so <code>pytest</code> works even before an editable install.)</p>"},{"location":"contributing/#before-opening-a-pull-request","title":"Before opening a pull request","text":"<ul> <li>Run the full suite and make sure it's green:</li> </ul> <pre><code>pytest\n</code></pre> <ul> <li> <p>Add tests for new behavior. The suite runs without any external tools, so new logic should be unit-tested against the <code>tmp_path</code> fixtures rather than a live cluster.</p> </li> <li> <p>Keep output directory and file names stable. Single-command reruns and SLURM array indexing depend on the existing layout (<code>RawData/</code>, <code>Trimmed/</code>, <code>Aligned/</code>, <code>TagDirs/</code>, <code>bedGraphs/</code>, <code>TSS/</code>, <code>QC/</code>) and on tag-directory / bedGraph naming (<code>&lt;sample&gt;_&lt;assay&gt;_&lt;replicate&gt;</code>, <code>&lt;sample&gt;_&lt;assay&gt;-combo</code>). Renaming any of these breaks reruns, so avoid it unless that's the explicit point of the change.</p> </li> </ul>"},{"location":"contributing/#reporting-issues","title":"Reporting issues","text":"<p>See Report an Issue.</p>"},{"location":"contributing/#code-style","title":"Code style","text":""},{"location":"contributing/#scope-notes","title":"Scope notes","text":"<ul> <li>HOMERun currently identifies every sample purely from its FASTQ filename. Manifest input (CSV / TSV / Excel) is out of scope for now.</li> </ul>"},{"location":"installation/","title":"Installation","text":"<p>HOMERun can be installed three ways. Conda is recommended, because it also installs the external bioinformatics tools HOMERun depends on (HOMER, STAR, HISAT2, samtools, skewer). The other two methods install only the Python package \u2014 you provide the tools yourself.</p>"},{"location":"installation/#recommended-conda-bioconda","title":"Recommended: Conda (Bioconda)","text":"<pre><code>conda install -c bioconda homerun\n</code></pre> <p>This pulls in every required tool automatically. Verify the install:</p> <pre><code>homerun --help\n</code></pre>"},{"location":"installation/#pypi-pip","title":"PyPI (pip)","text":"<pre><code>pip install homerun\n</code></pre> <p>pip installs the Python package only. The external tools must already be on your <code>PATH</code>. The easiest way is a dedicated conda environment:</p> <pre><code>conda create -n homerun-tools -c bioconda homer star hisat2 samtools skewer\n</code></pre>"},{"location":"installation/#from-source-git-clone","title":"From source (git clone)","text":"<pre><code>git clone https://github.com/NguyenThomas986/homerun.git\ncd homerun\npip install -e .\n</code></pre>"},{"location":"installation/#requirements","title":"Requirements","text":"<ul> <li>Python &gt;= 3.9</li> <li>A SLURM-based HPC cluster</li> <li>External tools: HOMER, STAR, HISAT2, samtools, skewer</li> </ul>"},{"location":"issue/","title":"Report an Issue","text":"<p>Found a bug or unexpected output? Report it one of two ways:</p> <ul> <li>Open a GitHub Issue</li> <li>Email thomas.nguyen11@wsu.edu</li> </ul>"},{"location":"issue/#what-to-include","title":"What to include","text":"<p>The more of this you can provide, the faster it can be diagnosed:</p> <ul> <li>Issue \u2014 a short description of the problem.</li> <li>Expected output \u2014 what you expected to happen.</li> <li>Result \u2014 what actually happened.</li> <li>SLURM log errors \u2014 any errors from the <code>.out</code> / <code>.err</code> job logs, if present.</li> <li>Command \u2014 the exact command you ran.</li> <li>Sample \u2014 the dataset used (organism, number of samples, genome build).</li> </ul>"},{"location":"issue/#template","title":"Template","text":"<p>Copy, paste, and fill in:</p> <pre><code>### Issue\n\n\n### Expected Output\n\n\n### Result\n\n\n### SLURM Log Errors (if any)\n\n\n### Command\n`your command here`\n\n\n### Sample\norganism / # samples / genome\n</code></pre>"},{"location":"pipeline/","title":"Pipeline","text":"<p>HOMERun analyzes csRNA-seq data as a series of steps, each handled by an established bioinformatics tool and submitted as SLURM jobs.</p>"},{"location":"pipeline/#1-adapter-trimming","title":"1. Adapter trimming","text":"<p>Sequencing reads carry adapter sequence that must be removed, and csRNA/sRNA reads are size-selected (roughly 20\u201358 nt).</p> <ul> <li>Tool: HOMER <code>homerTools trim</code> (csRNA, sRNA); skewer for paired-end totalRNA</li> </ul>"},{"location":"pipeline/#2-alignment-to-the-genome","title":"2. Alignment to the genome","text":"<p>Trimmed reads are mapped to a reference genome to determine where each read originated, including strand.</p> <ul> <li>Tool: STAR (default), HISAT2 (optional)</li> <li>Output: <code>Aligned/&lt;sample&gt;.Aligned.out.sam</code></li> </ul>"},{"location":"pipeline/#3-tag-directory-creation","title":"3. Tag directory creation","text":"<p>Alignments are converted into HOMER tag directories \u2014 position-sorted, depth-normalized read counts plus precomputed QC statistics.</p> <ul> <li>Tool: HOMER <code>makeTagDirectory</code></li> <li>Built per replicate (leaf) and merged per assay (combo)</li> </ul>"},{"location":"pipeline/#4-bedgraph-generation","title":"4. BedGraph generation","text":"<p>Genome-browser tracks are generated from the tag directories for visualizing signal along the genome.</p> <ul> <li>Tool: HOMER <code>makeUCSCfile</code></li> </ul>"},{"location":"pipeline/#5-tss-tsr-calling","title":"5. TSS / TSR calling","text":"<p>Capped-RNA peaks are called to identify active transcription start regions, using sRNA as an input control and totalRNA as a stability reference.</p> <ul> <li>Tool: HOMER <code>findcsRNATSS.pl</code></li> <li>Output: <code>Species/TSS/&lt;sample&gt;.tss.txt</code></li> </ul>"},{"location":"pipeline/#6-quality-control-reporting","title":"6. Quality control &amp; reporting","text":"<p>Per-sample QC is written under <code>Species/QC/&lt;sample&gt;/</code>. Compact cross-sample A/C/G/T nucleotide-divergence heatmaps (PNG/SVG plus TSV matrices) are written directly under <code>Species/QC/</code> for csRNA and sRNA.</p>"},{"location":"pipeline/#7-stability-analysis","title":"7. Stability analysis","text":"<p>Called TSRs are classified as stable or unstable transcripts using the totalRNA reference (HOMER <code>Log2Ratio vs. stableRNA</code>).</p>"},{"location":"quickstart/","title":"Quick Start","text":""},{"location":"quickstart/#run-on-slurm-recommended","title":"Run on SLURM (recommended)","text":"<p>Submit the pipeline as SLURM job arrays:</p> <pre><code>path/to/submit_array.sh \\\n  --project /path/to/project \\\n  --partition (parition_Name) \\\n  --conda-env (conda_ENV_Name) \\\n  --genome-index /path/to/STARindex \\\n  --genome mm10\n</code></pre>"},{"location":"quickstart/#run-directly","title":"Run directly","text":"<p>For a single node without job arrays:</p> <pre><code>homerun --project /path/to/project --genome mm10\n</code></pre> <p>or as a module:</p> <pre><code>python -m homerun --project /path/to/project --genome mm10\n</code></pre>"},{"location":"quickstart/#something-went-wrong","title":"Something went wrong?","text":"<p>See Report an Issue for how to report bugs and unexpected output.</p>"},{"location":"testing/","title":"Testing","text":"<p>Alpha</p> <p>HOMERun is in alpha testing. Test layout and coverage are still evolving.</p> <p>The test suite is pure Python: it needs no HOMER, STAR, HISAT2, or skewer, no network, and no SLURM. Every filesystem fixture uses a temporary directory, so the tests run anywhere from a fresh checkout. Fixtures that need external-tool output (FASTQ, SAM, HOMER tag directories) build minimal syntactically-valid fakes by hand instead of shelling out to real tools.</p>"},{"location":"testing/#install-test-dependencies","title":"Install test dependencies","text":"<pre><code>pip install -e \".[test]\"\n</code></pre>"},{"location":"testing/#run","title":"Run","text":"<pre><code>pytest\n</code></pre> <p>Options are preconfigured in <code>pyproject.toml</code> (<code>testpaths = [\"tests\"]</code>, <code>-ra --strict-markers</code>), so a bare <code>pytest</code> from the repo root runs everything.</p> <p>Run a subset:</p> <pre><code>pytest tests/test_cli.py        # one file\npytest -k precedence            # tests matching a keyword\n</code></pre> <p>Slow tests are marked; select or skip them:</p> <pre><code>pytest -m \"not slow\"            # skip slow tests\npytest -m slow                  # run only slow tests\n</code></pre> <p>Because <code>--strict-markers</code> is on, any marker not registered in <code>pyproject.toml</code> is an error \u2014 register new markers there before using them.</p>"},{"location":"testing/#whats-covered","title":"What's covered","text":"<ul> <li>CLI parsing and <code>main()</code> fast paths \u2014 argparse construction, <code>--help</code>, exit codes, and the <code>--count-samples</code> / <code>--count-groups</code> / <code>--check-rerun</code> / <code>--stage-raw</code> controller paths that a SLURM wrapper depends on, none of which run the pipeline.</li> <li>Configuration \u2014 flag &gt; env &gt; default precedence, the <code>_pick()</code> helper, <code>SLURM_CPUS_PER_TASK</code> fallback, and path-builder correctness.</li> <li>Filename identity \u2014 <code>parse_sample_name</code>, <code>assay_of_leaf</code> / <code>seq_type</code>, replicate markers, and identity-based R2 mate-pairing (including the ENCODE mismatched-accession regression).</li> <li>Package imports \u2014 every public module imports cleanly and <code>__version__</code> is accessible.</li> </ul>"},{"location":"testing/#fixtures-testsconftestpy","title":"Fixtures (<code>tests/conftest.py</code>)","text":"<ul> <li><code>project_dir</code> \u2014 an empty temporary project root.</li> <li><code>make_cfg</code> / <code>cfg</code> \u2014 a real <code>Config</code> pointed at a temporary project, any field overridable.</li> <li><code>make_fastq</code> \u2014 writes a minimal valid FASTQ.</li> <li><code>staged_sample</code> \u2014 a <code>Config</code> plus one already-staged csRNA replicate.</li> <li><code>paired_totalrna_sample</code> \u2014 a staged totalRNA replicate with both mates present.</li> <li><code>non_interactive_matplotlib</code> \u2014 forces matplotlib's headless <code>Agg</code> backend so plotting tests never open a GUI window.</li> </ul>"},{"location":"testing/#writing-new-tests","title":"Writing new tests","text":"<ul> <li>Use <code>tmp_path</code> and the provided fixtures; never touch absolute paths or the network.</li> <li>Build fake tool outputs by hand (see <code>make_fastq</code>) rather than calling real tools.</li> <li>Register any new marker in <code>pyproject.toml</code>, or <code>--strict-markers</code> will fail the run.</li> </ul>"}]}